import * as React from "react";
import Layout from "../components/layout";

const Contact = () => {
  return <Layout pageTitle="Contact Us">This is the contact us page</Layout>;
};

export default Contact;
